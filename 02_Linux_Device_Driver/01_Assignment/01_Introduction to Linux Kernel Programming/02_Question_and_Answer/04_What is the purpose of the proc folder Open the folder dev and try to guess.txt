1./proc is a virtual filesystem that shows information about the running system and kernel.

2.It doesn’t contain real files on disk, but files generated by the kernel.

Examples:

/proc/cpuinfo → info about CPU

/proc/meminfo → info about memory

/proc/[pid] → info about a specific process